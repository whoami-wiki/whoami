# Review Talk Pages

Check all talk pages for pending questions or suggestions.

## Process
1. Run `wai changes --limit 50` to find recently active pages
2. For each page, run `wai talk read "<page>"` to check for open threads
3. Summarize pending items that need human input
4. Present them for response
